description: Bridgetek EVE display controller

compatible: "bridgetek,eve"

include: spi-device.yaml

properties:
  reg:
    required: true

  pd-gpios:
    type: phandle-array
    required: true
    description: Power down pin

  int-gpios:
    type: phandle-array
    required: true
    description: Interrupt pin 